[build-system]
requires = ["setuptools>=70"]
build-backend = "setuptools.build_meta"

[project]
name = "solana-memecoin-intelligence"
version = "1.5.0"
description = "Read-only Solana and BNB ultra-early Discord intelligence service"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "aiohttp>=3.10,<4",
  "discord.py==2.7.1",
]

[project.optional-dependencies]
dev = ["pytest>=8", "pytest-asyncio>=0.23", "ruff>=0.6"]
research = ["duckdb>=1.4,<2", "pytz>=2024.1", "scikit-learn>=1.5,<2"]
social = ["telethon>=1.36,<2"]

[project.scripts]
memecoin-bot = "memecoin_bot.main:main"
gambit-history = "memecoin_bot.historical.cli:main"
gambit-convergence = "memecoin_bot.convergence.__main__:main"
gambit-e4 = "memecoin_bot.e4_exec.__main__:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
memecoin_bot = ["historical/migrations/**/*.sql", "historical/sql/dune/*.sql", "historical/sql/dune/*.json"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
line-length = 100
target-version = "py311"
