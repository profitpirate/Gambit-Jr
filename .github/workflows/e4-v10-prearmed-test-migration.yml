name: E4 V10 authenticated prearmed test migration

on:
  push:
    branches:
      - codex/e4-v10-three-pipelines
    paths:
      - scripts/e4_migrate_prearmed_test_v10.py
      - .github/workflows/e4-v10-prearmed-test-migration.yml

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/e4-v10-three-pipelines
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python scripts/e4_migrate_prearmed_test_v10.py
      - name: Commit migrated prearmed fixture
        run: |
          if git diff --quiet -- tests/test_e4_hardening_v6.py src/memecoin_bot/e4_hardening_v10.py; then
            echo "No migration changes"
            exit 0
          fi
          git config user.name "gambit-e4-v10"
          git config user.email "actions@users.noreply.github.com"
          git add tests/test_e4_hardening_v6.py src/memecoin_bot/e4_hardening_v10.py
          git commit -m "test(e4): use authenticated launch intent in prearmed fixture"
          git push origin HEAD:codex/e4-v10-three-pipelines
